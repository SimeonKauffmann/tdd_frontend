import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ProductCard({}: Props): ReactElement {
    return (
        <div>
            Card
        </div>
    )
}
