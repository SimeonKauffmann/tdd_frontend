import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ListView({}: Props): ReactElement {
    return (
        <div>
            This is a list
        </div>
    )
}
