import React, { ReactElement } from 'react'

interface Props {
    
}

export default function LoginView({}: Props): ReactElement {
    return (
        <div>
            Login here
        </div>
    )
}
