import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ProductView({}: Props): ReactElement {
    return (
        <div>
            Product view
        </div>
    )
}
